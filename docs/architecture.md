# Lab Architecture

```text
MacBook Air M1 (macOS)
│
├── UTM
│
├── Ubuntu Server
│      │
│      └── Splunk Enterprise
│
├── Windows 11 ARM
│
└── macOS Terminal
        │
        └── SSH → Ubuntu Server
```
